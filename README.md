# homebrew-agentctl

Public Homebrew tap for AgentCTL.

```bash
brew install rxdn-forge/agentctl/agentctl
brew upgrade agentctl
```

The private source repository publishes release artifacts to
`rxdn-forge/agentctl-dist`; this tap only carries the generated Homebrew formula
that points at those public artifacts.
